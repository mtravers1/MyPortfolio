version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install --legacy-peer-deps
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next   # use "build" if Create React App
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
